#include <bits/stdc++.h>
using namespace std;

int n;
int board[1005][1005] = {0};
int ans = 0;

void solve(int i, int j)
{
    // base case
    if ()

    // recursive case
}

int main()
{
    cin >> n;
    for (int i = 1; i <= n; i++)
    {
        for (int j = 1; j <= n; j++)
        {
            cin >> board[i][j];
        }
    }

    solve(1, 1);
    cout << ans;
    return 0;
}